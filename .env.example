# ==============================================
# APPLICATION CONFIGURATION
# ==============================================
NODE_ENV=development

# Server Configuration
# Production: REQUIRED
HOST=localhost
PORT=3000

# Application URL (used for email links and CORS)
# Production: REQUIRED
APP_URL=http://localhost:3000

APP_NAME="Blog Platform"
API_PREFIX=api

# ==============================================
# DATABASE CONFIGURATION (PostgreSQL)
# ==============================================
# Docker Database Credentials (used by docker-compose.yml)
DB_USER=blog_user
DB_PASSWORD=blog_password_change_in_production
DB_NAME=blog
DB_HOST=localhost
DB_PORT=5432

# Prisma Database URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# IMPORTANT: Update this with actual values from DB_* variables above
DATABASE_URL="postgresql://blog_user:blog_password_change_in_production@localhost:5432/blog?schema=public"

# ==============================================
# JWT AUTHENTICATION
# ==============================================
# IMPORTANT: Change these in production!
# Access token (short-lived, e.g., 1 hour)
JWT_SECRET="your-super-secret-jwt-key-min-32-characters-change-in-production"
JWT_EXPIRES_IN="1h"

# Refresh token (long-lived, e.g., 7 days)
JWT_REFRESH_SECRET="your-super-secret-jwt-refresh-key-min-32-characters-change-in-production"
JWT_REFRESH_EXPIRES_IN="7d"

# ==============================================
# CORS CONFIGURATION
# ==============================================
# Comma-separated list of allowed origins
CORS_ORIGIN="http://localhost:3000,http://localhost:3001"

# ==============================================
# RATE LIMITING
# ==============================================
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# ==============================================
# LOGGING
# ==============================================
# Levels: error, warn, info, debug, verbose
LOG_LEVEL=info

# ==============================================
# REDIS (For BullMQ Queue & Caching)
# ==============================================
# Development: Optional (defaults to localhost:6379)
# Production: REQUIRED
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ==============================================
# AWS S3 (Optional - For Future File Upload)
# ==============================================
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_BUCKET=blog-uploads

# ==============================================
# EMAIL CONFIGURATION
# ==============================================
# Mail provider to use: 'mailgun' (default) or 'sendgrid'
MAIL_PROVIDER=mailgun

# --- Mailgun (Primary Provider) ---
# Get your API key from: https://app.mailgun.com/settings/api_security
# Get your domain from: https://app.mailgun.com/sending/domains
# SECURITY: In production, MAILGUN_FROM_EMAIL is REQUIRED (no default)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_FROM_EMAIL=noreply@yourdomain.com

# --- SendGrid (Optional Provider) ---
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# SECURITY: In production, SENDGRID_FROM_EMAIL is REQUIRED (no default)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# ==============================================
# ADMIN CONFIGURATION
# ==============================================
# Comma-separated list of admin emails for reports
ADMIN_REPORT_RECIPIENTS=
